DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Optional single-password gate (leave APP_PASSWORD unset to disable auth entirely).
# APP_PASSWORD="change-me"
# SESSION_SECRET="a-random-32-char-minimum-secret-string-here"
